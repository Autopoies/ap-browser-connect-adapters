site: weread
name: ranking
description: Fetch visible WeRead ranking books

args:
  limit:
    type: int
    default: 20
    desc: max books

output:
  type: array
  fields: [title, author, url]

steps:
  - goto: "https://weread.qq.com/web#ranking"
  - wait: "body"
  - eval: ranking.js
