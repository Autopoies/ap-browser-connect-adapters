site: chatgpt
name: models
description: >-
  List available ChatGPT models, thinking efforts, and modes with indices

output:
  type: object
  fields: [currentModel, currentEffort, models, efforts, presets, modes]

steps:
  - eval: models.js
