site: chatgpt
name: models
description: List available ChatGPT models, thinking efforts, and modes with their 0-based indices

output:
  type: object
  fields: [currentModel, currentEffort, models, efforts, modes]

steps:
  - eval: models.js
